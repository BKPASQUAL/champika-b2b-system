/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your existing config options go here
  // Example:
  // images: {
  //   remotePatterns: [ ... ],
  // },
};

export default nextConfig;